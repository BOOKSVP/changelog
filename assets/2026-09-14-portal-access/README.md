# Portal Access Screenshots

This directory should contain the following images:

1. `1_locked.png` - Locked portal screen with "Request access" button for Chase Gallery
2. `5_accept.png` - Portal access request accept screen for Chase Gallery

These images show the new portal access request feature where:
- Locked-out visitors can request access via a "Request access" button
- Organisation owners receive requests and can accept with role selection
